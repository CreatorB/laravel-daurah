<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Supported UI locales for the questions feature
    |--------------------------------------------------------------------------
    | Each locale controls RTL via app()->setLocale + dir attribute in Blade.
    */
    'locales' => ['ar', 'id'],

    /*
    |--------------------------------------------------------------------------
    | Per-direction map for the supported locales
    |--------------------------------------------------------------------------
    */
    'direction' => [
        'ar' => 'rtl',
        'id' => 'ltr',
    ],

    /*
    |--------------------------------------------------------------------------
    | Maximum question_body length (used in validation + UI counter)
    |--------------------------------------------------------------------------
    | The DB column is mediumText (16MB) so this is only a UI/validation cap.
    */
    'max_length' => 20000,

    /*
    |--------------------------------------------------------------------------
    | Pagination size for public listing
    |--------------------------------------------------------------------------
    */
    'per_page' => 10,

    /*
    |--------------------------------------------------------------------------
    | Pagination size for admin dashboard
    |--------------------------------------------------------------------------
    */
    'admin_per_page' => 15,
];
